For all readers

The App folder contains keyboard shortcuts, handlers, functions, and states.

The configuration contains the encrypted code of the bot

The Id_Name_admin_users folder contains a file in a json format. It contains the id and name of the users who have access to editing from under the bot.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
For Russian readers

В папке App содержатся клавиотуры, хендлеры, функции и состояния.

В конфигурации содержится зашифрованный код бота

В папке Id_Name_admin_users содержится файл в фомате json. В нем записаны id и имя пользавателей, у которых есть допуск к редактированию из под бота.